A little shared random timer: enter min/max seconds and countdown.
Runs the timer in secret until the countdown, then it says how many seconds left.
Should work across browsers if they have the same ?room=<room number> as part of url.
